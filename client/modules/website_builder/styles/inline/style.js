/* Images */
let 
	imgUrl = '/images/weights.jpg',
	imgUrl1 = '/images/static-hero.png'
;

let styles = {
  backgroundColor: 'orange',
  backgroundImage: 'url(' + imgUrl + ')'
};

let styles1 = {
	backgroundImage: 'url(' + imgUrl1 + ')'
}

export default styles;